# Start the implementation

The repository name omitted from the request is resolved to **AI-Ascension/ascension-brand-overhaul**. This is the new canonical home for brand sources, publishing tools, campaign materials, integration manifests, and rollout evidence; it does not absorb runtime implementation code from other repositories.

Give an execution-capable agent the extracted directory and this instruction:

> Read MASTER_PROMPT.md and execute it. Use the full package as the authoritative implementation brief. Create or safely reuse AI-Ascension/ascension-brand-overhaul, then implement and integrate the overhaul with real three-level descendants: Luna Max for ordinary roles, Astra for the two art-prompt-author leaves, and gpt-image-2 for all artwork. Preserve existing working code and evidence. Continue all unblocked work through implementation, testing, and independent review. Do not stop at a plan or claim that unapproved publication has occurred.

The root should first inspect the applicable local and target-repository AGENTS.md files. Candidate runtime settings in `orchestration/runtime-config.example.toml` must be checked against the installed client before use; do not blindly replace global configuration. The root model remains unchanged. Every ordinary descendant must request and verify Luna/max; the two designated art-author leaves must instead request and verify Astra/max.

The native session must actually permit descendants to spawn descendants. The package cannot create that capability through prompt text. Record requested, accepted, and observed settings separately, along with real parent-child identifiers. If access is missing, record the exact failure and keep completing independent authorized tasks without mislabeling the orchestration.

Use `art/asset-board.html` in a browser for the art map, or read `art/ART_MAP.md`. Every new-art row requires Astra-authored gpt-image-2 generation. Authentic evidence and historical references are separate inventories; neither can masquerade as new generated art. Existing references are not bundled and new art is not already delivered.

After an interruption, use `prompts/CONTINUE.md`. After integration, use `prompts/AUDIT_AND_REPAIR.md`. Use `prompts/RELEASE.md` only within already authorized operational scope. The release prompt cannot grant permission on the user's behalf.

The art policy replaces the older deterministic/vector/reuse lanes. Read `REVISION_NOTES.md` and `art/ASTRA_GPT_IMAGE_2_POLICY.md` before executing any art task. Keep the 49-role topology and 250-open-descendant budget; do not change every implementation agent to Astra.
