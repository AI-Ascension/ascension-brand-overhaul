# Asset briefs for the Astra prompt author — gpt-image-2 only

These are bounded creative briefs, not final image-tool prompts and not evidence of an actual Astra session. The designated Astra author must independently write and save each final prompt and each edit prompt using ASTRA_ART_PROMPT_AUTHOR.md. Every new artwork uses gpt-image-2. No drawing/render/reuse alternatives remain.

Read ART_BRIEF.md and ASTRA_GPT_IMAGE_2_POLICY.md. Retain exact source-copy contracts, actual supplied references, generated-versus-evidence classification, native author/model metadata, raw outputs, hashes, and independent review. Export sizes below are final targets, not guaranteed valid native API sizes. Only noncreative transformations are allowed.


## ID-01 — Branching-A mark

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GLOBAL.

### Brief supplied to Astra

Create an original upward A from two or three branching paths with one continuous highlighted ascent. No copied existing trademark or game tower. One-color silhouette must remain distinct at 16 px. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/mark.png` — 64×64; generated_art_export.
- `brand/assets/identity/mark-mono.png` — 64×64; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-01/`. Final Astra prompt records: `brand/prompts/astra/ID-01/`. Generation provenance: `brand/provenance/generations/ID-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-02 — Organization wordmark

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GLOBAL.

### Brief supplied to Astra

Precisely typeset AI Ascension with a consistent mark gap and clearspace. Astra must provide exact literal text to gpt-image-2; retain the prompt and raster output as production sources, without bundling fonts or claiming final vector outlines. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/ai-ascension-light.png` — 1200×220; generated_art_export.
- `brand/assets/identity/ai-ascension-dark.png` — 1200×220; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-02/`. Final Astra prompt records: `brand/prompts/astra/ID-02/`. Generation provenance: `brand/provenance/generations/ID-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-03 — Flagship product lockup

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BUILD, GITHUB.

### Brief supplied to Astra

Typeset Ascension as the flagship, with subtle by AI Ascension attribution only where needed. Do not create a second unrelated logo. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/ascension-light.png` — 1000×220; generated_art_export.
- `brand/assets/identity/ascension-dark.png` — 1000×220; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-03/`. Final Astra prompt records: `brand/prompts/astra/ID-03/`. Generation provenance: `brand/provenance/generations/ID-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-04 — The Climb title lockup

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** WATCH, CAMPAIGN, BROADCAST.

### Brief supplied to Astra

Editorial title The Climb with small by AI Ascension endorsement. Same path geometry, no implication of game publisher affiliation. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/the-climb-master.png` — 1400×400; generated_art_export.
- `brand/assets/identity/the-climb.png` — 1400×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-04/`. Final Astra prompt records: `brand/prompts/astra/ID-04/`. Generation provenance: `brand/provenance/generations/ID-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-05 — Organization avatar set

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GLOBAL, GITHUB, CAMPAIGN.

### Brief supplied to Astra

Have gpt-image-2 generate the approved branching mark within crop-safe square and circular boundaries; inspect at 24, 48, and 96 px. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/avatar-1024.png` — 1024×1024; generated_art_export.
- `brand/assets/identity/avatar-256.png` — 256×256; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-05/`. Final Astra prompt records: `brand/prompts/astra/ID-05/`. Generation provenance: `brand/provenance/generations/ID-05/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-06 — Browser and app icon set

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GLOBAL.

### Brief supplied to Astra

A simplified small-size mark, not the full wordmark. Test browser tabs in light/dark surfaces and avoid hairline details. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/favicon.png` — 64×64; generated_art_export.
- `brand/assets/identity/favicon-32.png` — 32×32; generated_art_export.
- `brand/assets/identity/favicon-16.png` — 16×16; generated_art_export.
- `brand/assets/identity/apple-touch-icon.png` — 180×180; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-06/`. Final Astra prompt records: `brand/prompts/astra/ID-06/`. Generation provenance: `brand/provenance/generations/ID-06/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-07 — Product and evidence icon sprite

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GLOBAL, DOCS, RUN.

### Brief supplied to Astra

Create labeled icon roles for play, record, replay, code, evidence, warning, unavailable, external link, privacy, and intervention. Evidence dimensions need distinct labels; no single green badge claims universal verification. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/icons.png` — 1024×1024 icon sheet; approved generated icon slices at 24×24 and 48×48; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-07/`. Final Astra prompt records: `brand/prompts/astra/ID-07/`. Generation provenance: `brand/provenance/generations/ID-07/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## ID-08 — Design token and typography specimen

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** PRESS, DOCS.

### Brief supplied to Astra

Show palette, spacing, button states, typography references, accessibility combinations, and mark rules. Reference existing fonts; bundle no font binaries. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01, ID-02, ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/identity/specimen.html` — responsive document; functional_document.
- `brand/assets/identity/specimen.png` — 1600×2000; generated_art_export.

Raw generation outputs: `brand/sources/generated/ID-08/`. Final Astra prompt records: `brand/prompts/astra/ID-08/`. Generation provenance: `brand/provenance/generations/ID-08/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## BG-01 — Editorial ascent environment master

**Author:** `W02-C2-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** HOME, PRESS.

### Brief supplied to Astra

Original restrained editorial environment: warm paper texture, abstract branching paths rising toward soft amber light, ink-like geometry, broad clean negative space on left. No actual game scene, logos, letters, UI, numbers, people, or simulated result. This is clearly brand illustration. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/illustration/ascent-master.png` — 2400×1600; generated_art_export.

Raw generation outputs: `brand/sources/generated/BG-01/`. Final Astra prompt records: `brand/prompts/astra/BG-01/`. Generation provenance: `brand/provenance/generations/BG-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## BG-02 — Dark broadcast atmosphere master

**Author:** `W02-C2-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST, WATCH.

### Brief supplied to Astra

Original very subtle near-black charcoal/paper atmosphere with a single amber branching ascent at the far right. Keep center low-contrast and empty for real footage or readable copy. No text, scoreboard, game art, provider logos, or fake gameplay. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/illustration/broadcast-master.png` — 2560×1440; generated_art_export.

Raw generation outputs: `brand/sources/generated/BG-02/`. Final Astra prompt records: `brand/prompts/astra/BG-02/`. Generation provenance: `brand/provenance/generations/BG-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## BG-03 — Series interstitial texture

**Author:** `W02-C2-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN, BROADCAST.

### Brief supplied to Astra

Quiet warm off-white editorial paper field with fine abstract branching contour geometry entering from bottom edge. Enough negative space for exact source-validated titles. Original restrained illustration; no mystical crown/flame or AI-brain cliché. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/illustration/series-master.png` — 2160×2160; generated_art_export.

Raw generation outputs: `brand/sources/generated/BG-03/`. Final Astra prompt records: `brand/prompts/astra/BG-03/`. Generation provenance: `brand/provenance/generations/BG-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## BG-04 — Light/dark route pattern

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GLOBAL, CAMPAIGN.

### Brief supplied to Astra

Have the Astra author specify a sparse repeatable branching line motif from the identity geometry. Decorative only; it must not resemble an actual gameplay route map. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/illustration/route-pattern-light.png` — 800×800 tile; generated_art_export.
- `brand/assets/illustration/route-pattern-dark.png` — 800×800 tile; generated_art_export.

Raw generation outputs: `brand/sources/generated/BG-04/`. Final Astra prompt records: `brand/prompts/astra/BG-04/`. Generation provenance: `brand/provenance/generations/BG-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-01 — Homepage illustration crop set

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** HOME.

### Brief supplied to Astra

Crop the approved editorial master differently for desktop and mobile. Keep all factual text as HTML. Prefer real gameplay poster as primary product evidence when available. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets BG-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/home-desktop.webp` — 1600×1000; generated_art_export.
- `brand/assets/web/home-mobile.webp` — 800×1000; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-01/`. Final Astra prompt records: `brand/prompts/astra/WEB-01/`. Generation provenance: `brand/provenance/generations/WEB-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-02 — Latest-run generated editorial poster

**Author:** `W02-C2-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** HOME, WATCH, RUN.

### Brief supplied to Astra

Generate an original editorial poster for the latest approved run. Use only source-verified literal title/outcome text and original branching-path imagery. Label it as an illustration; do not imitate a gameplay screenshot, fabricate a game state, or use a generated poster as evidence. Link separately to authentic captures in RUN-06. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/latest-run-poster.webp` — 1600×900; generated_art_export.
- `brand/assets/web/latest-run-poster-mobile.webp` — 900×1200; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-02/`. Final Astra prompt records: `brand/prompts/astra/WEB-02/`. Generation provenance: `brand/provenance/generations/WEB-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_prompt_image_model_factual_copy_rights_and_publication_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-03 — Watch and Build pathway graphics

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** HOME, BUILD.

### Brief supplied to Astra

Small functional diagrams: recorded run leading to evidence; code leading to tested integration. Match actual product capability, no fabricated UI. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/path-watch.png` — 800×500; generated_art_export.
- `brand/assets/web/path-build.png` — 800×500; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-03/`. Final Astra prompt records: `brand/prompts/astra/WEB-03/`. Generation provenance: `brand/provenance/generations/WEB-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-04 — Hero and primary-page social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** HOME, SOCIAL_PREVIEW.

### Brief supplied to Astra

Deterministically compose the actual brand promise, mark, and a clearly illustrative backdrop. No unmeasured stats or implied live state. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/site-og.png` — 1200×630; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-04/`. Final Astra prompt records: `brand/prompts/astra/WEB-04/`. Generation provenance: `brand/provenance/generations/WEB-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-05 — 404 and unavailable artwork

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** EMPTY.

### Brief supplied to Astra

Simple interrupted path with humane empty-state copy supplied in HTML, not embedded. Keep an obvious next action in the page. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/404.png` — 800×600; generated_art_export.
- `brand/assets/web/unavailable.png` — 800×600; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-05/`. Final Astra prompt records: `brand/prompts/astra/WEB-05/`. Generation provenance: `brand/provenance/generations/WEB-05/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-06 — Subscription success and confirmation art

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** SUBSCRIBE.

### Brief supplied to Astra

Minimal envelope/path/check illustrations; do not show success before the server confirms the actual corresponding state. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/subscribe-confirm.png` — 400×300; generated_art_export.
- `brand/assets/web/subscribe-success.png` — 400×300; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-06/`. Final Astra prompt records: `brand/prompts/astra/WEB-06/`. Generation provenance: `brand/provenance/generations/WEB-06/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-07 — Current capability diagram

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BUILD, DOCS.

### Brief supplied to Astra

Have gpt-image-2 generate from the current reviewed capability registry. Source/report/public artifact/support dimensions stay distinct. Supply an accessible table. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/capabilities.png` — 1400×900; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-07/`. Final Astra prompt records: `brand/prompts/astra/WEB-07/`. Generation provenance: `brand/provenance/generations/WEB-07/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_current_capability_source_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## WEB-08 — Map viewer integration preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** MAP, RUN.

### Brief supplied to Astra

Use reviewed output of the real map viewer when ready. Otherwise generate a boldly labeled conceptual synthetic route map only on a design/experimental surface; never a live game screenshot. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/web/map-preview.png` — 1440×960; generated_art_export.
- `brand/assets/web/map-preview-mobile.png` — 900×1200; generated_art_export.

Raw generation outputs: `brand/sources/generated/WEB-08/`. Final Astra prompt records: `brand/prompts/astra/WEB-08/`. Generation provenance: `brand/provenance/generations/WEB-08/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_map_owner_review_and_mode_label. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## RUN-01 — Run summary card template

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** RUN, SOCIAL_PREVIEW.

### Brief supplied to Astra

Data-bound run ID, actual outcome, tested model config, game/build, intervention indicator, evidence availability, and source URL. No invented leaderboard. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/run/summary-template.png` — 1200×630; generated_art_export.
- `brand/assets/run/summary-template-square.png` — 1080×1080; generated_art_export.

Raw generation outputs: `brand/sources/generated/RUN-01/`. Final Astra prompt records: `brand/prompts/astra/RUN-01/`. Generation provenance: `brand/provenance/generations/RUN-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_template_review_then_manifest_approval_per_instance. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## RUN-02 — Decision reveal card templates

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DECISION, CAMPAIGN.

### Brief supplied to Astra

Create before/reveal variants with exact state/options/choice/consequence from public records. Separate agent action, public explanation, and human analysis. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/run/decision-landscape.png` — 1200×630; generated_art_export.
- `brand/assets/run/decision-square.png` — 1080×1080; generated_art_export.
- `brand/assets/run/decision-vertical.png` — 1080×1920; generated_art_export.

Raw generation outputs: `brand/sources/generated/RUN-02/`. Final Astra prompt records: `brand/prompts/astra/RUN-02/`. Generation provenance: `brand/provenance/generations/RUN-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_approved_decision_data_per_instance. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## RUN-03 — Timeline graphic template

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** RUN, DOCS.

### Brief supplied to Astra

Have gpt-image-2 generate ordered public events and interventions with real timestamps/indices. No smoothing, invented gaps, or misaligned video markers. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/run/timeline-template.png` — 1600×500; generated_art_export.

Raw generation outputs: `brand/sources/generated/RUN-03/`. Final Astra prompt records: `brand/prompts/astra/RUN-03/`. Generation provenance: `brand/provenance/generations/RUN-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_approved_run_manifest_per_instance. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## RUN-04 — Replay receipt graphic template

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** RUN, CAMPAIGN.

### Brief supplied to Astra

Show actual replay outcome and matching policy. Distinguish zero new provider calls from original calls and from compute cost. Include source and evidence availability. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/run/replay-receipt.png` — 1200×630; generated_art_export.

Raw generation outputs: `brand/sources/generated/RUN-04/`. Final Astra prompt records: `brand/prompts/astra/RUN-04/`. Generation provenance: `brand/provenance/generations/RUN-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_approved_replay_record_per_instance. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## RUN-05 — Comparison and rules panel template

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** RUN, WATCH.

### Brief supplied to Astra

Explicitly show matching/differing versions, seed visibility, observation/action interfaces, resource budgets, interventions, and attempts. No general intelligence ranking. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/run/comparison-template.png` — 1600×900; generated_art_export.

Raw generation outputs: `brand/sources/generated/RUN-05/`. Final Astra prompt records: `brand/prompts/astra/RUN-05/`. Generation provenance: `brand/provenance/generations/RUN-05/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_comparison_protocol_and_data_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-01 — Single-agent broadcast overlay

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST.

### Brief supplied to Astra

Game dominates; side panel exposes current public model config, floor/health/action/status. Separate action, explanation, and intervention. No hard-coded invented values. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-04, ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/single-agent-master.png` — 1920×1080; generated_art_export.
- `brand/assets/broadcast/single-agent.png` — 1920×1080 transparent; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-01/`. Final Astra prompt records: `brand/prompts/astra/CAST-01/`. Generation provenance: `brand/provenance/generations/CAST-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-02 — Comparison broadcast overlay

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST.

### Brief supplied to Astra

Directed comparison scene with two readable views and configuration labels. Do not make four unreadable streams the default. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets CAST-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/comparison.png` — 1920×1080; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-02/`. Final Astra prompt records: `brand/prompts/astra/CAST-02/`. Generation provenance: `brand/provenance/generations/CAST-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-03 — Mobile broadcast layout

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST, CAMPAIGN.

### Brief supplied to Astra

One legible game view with safe subtitle area and a concise status strip; test actual footage crop before approval. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets CAST-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/vertical.png` — 1080×1920; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-03/`. Final Astra prompt records: `brand/prompts/astra/CAST-03/`. Generation provenance: `brand/provenance/generations/CAST-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-04 — Starting, intermission, and ended scenes

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST.

### Brief supplied to Astra

Distinct accurate operating states using source-validated copy over the approved dark master. A starting scene does not claim a feed is already live. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets BG-02, ID-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/starting.png` — 1920×1080; generated_art_export.
- `brand/assets/broadcast/intermission.png` — 1920×1080; generated_art_export.
- `brand/assets/broadcast/ended.png` — 1920×1080; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-04/`. Final Astra prompt records: `brand/prompts/astra/CAST-04/`. Generation provenance: `brand/provenance/generations/CAST-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-05 — Lower thirds and disclosure bars

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST.

### Brief supplied to Astra

Name, role, actual model config, human intervention, retrospective analysis, and sponsor disclosure variants. Keep descriptions truthful and readable. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-07 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/lower-third.png` — 1920×240; generated_art_export.
- `brand/assets/broadcast/disclosure.png` — 1920×160; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-05/`. Final Astra prompt records: `brand/prompts/astra/CAST-05/`. Generation provenance: `brand/provenance/generations/CAST-05/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-06 — Short title transition

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** BROADCAST, CAMPAIGN.

### Brief supplied to Astra

Use Astra-authored gpt-image-2 generations for every title/path visual. Mechanically sequence or fade approved generated frames into a transition; provide reduced-motion still. No flashing or synthetic gameplay. No music requirement or unlicensed audio. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-04, BG-03 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/title-transition.webm` — 1920×1080; 1–2 s; mechanical_motion_from_generated_art.
- `brand/assets/broadcast/title-transition-still.png` — 1920×1080; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-06/`. Final Astra prompt records: `brand/prompts/astra/CAST-06/`. Generation provenance: `brand/provenance/generations/CAST-06/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## CAST-07 — Operator scene map

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DOCS.

### Brief supplied to Astra

Document scene transitions and missing-stream states as an operator diagram. This is functional documentation, not promotional evidence. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/broadcast/scene-map.png` — 1600×1000; generated_art_export.

Raw generation outputs: `brand/sources/generated/CAST-07/`. Final Astra prompt records: `brand/prompts/astra/CAST-07/`. Generation provenance: `brand/provenance/generations/CAST-07/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-01 — Channel cover

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN.

### Brief supplied to Astra

The Climb by AI Ascension, restrained path illustration, central crop-safe composition. Recheck current platform crop behavior. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-04, BG-02 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/channel-cover.png` — 2560×1440; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-01/`. Final Astra prompt records: `brand/prompts/astra/MKT-01/`. Generation provenance: `brand/provenance/generations/MKT-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-02 — Social header

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN.

### Brief supplied to Astra

Clear organization brand and concise series promise; no dates or stats unless an approved campaign instance supplies them. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/social-header.png` — 1500×500; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-02/`. Final Astra prompt records: `brand/prompts/astra/MKT-02/`. Generation provenance: `brand/provenance/generations/MKT-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-03 — Thumbnail system

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN, WATCH.

### Brief supplied to Astra

Two generated layouts: actual outcome and an honest decision question. Use original generated editorial imagery informed by rights-reviewed source context, not a fabricated capture; truthful headline no more than a few clear words. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/thumbnail-outcome.png` — 1280×720; generated_art_export.
- `brand/assets/marketing/thumbnail-question.png` — 1280×720; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-03/`. Final Astra prompt records: `brand/prompts/astra/MKT-03/`. Generation provenance: `brand/provenance/generations/MKT-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_per_episode_source_and_copy_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-04 — Season key visual

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN, PRESS.

### Brief supplied to Astra

Road to the First Verified Win, updated if real evidence makes it obsolete. Branded illustrative composition, not a manufactured victory scene. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-04, BG-03 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/season-key-visual.png` — 1920×1080; generated_art_export.
- `brand/assets/marketing/season-key-visual-vertical.png` — 1080×1920; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-04/`. Final Astra prompt records: `brand/prompts/astra/MKT-04/`. Generation provenance: `brand/provenance/generations/MKT-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-05 — Community challenge card

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN, DECISION.

### Brief supplied to Astra

Use a real approved challenge, actual rules, input deadline only if scheduled, and source link. No invented votes. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets RUN-02 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/challenge-template.png` — 1080×1080; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-05/`. Final Astra prompt records: `brand/prompts/astra/MKT-05/`. Generation provenance: `brand/provenance/generations/MKT-05/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-06 — Release and contributor card

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, CAMPAIGN.

### Brief supplied to Astra

Release badge driven by actual version/release state; contributor name/photo only with permission. No fictitious testimonial. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-03 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/release-template.png` — 1200×630; generated_art_export.
- `brand/assets/marketing/contributor-template.png` — 1080×1080; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-06/`. Final Astra prompt records: `brand/prompts/astra/MKT-06/`. Generation provenance: `brand/provenance/generations/MKT-06/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-07 — Email digest artwork

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** SUBSCRIBE.

### Brief supplied to Astra

Lightweight image with HTML/text fallback, no tracking pixels or text-only-in-image body. Run card values require approved manifest. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, RUN-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/email-header.png` — 1200×320; generated_art_export.
- `brand/assets/marketing/email-run-card.png` — 1200×630; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-07/`. Final Astra prompt records: `brand/prompts/astra/MKT-07/`. Generation provenance: `brand/provenance/generations/MKT-07/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-08 — Sponsorship disclosure tile

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** CAMPAIGN, PRESS.

### Brief supplied to Astra

Neutral generated disclosure template. Do not include real sponsor marks or imply a deal until documented. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/marketing/sponsor-disclosure.png` — 1200×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/MKT-08/`. Final Astra prompt records: `brand/prompts/astra/MKT-08/`. Generation provenance: `brand/provenance/generations/MKT-08/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## MKT-09 — Press-kit cover and index

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** PRESS.

### Brief supplied to Astra

Present approved downloadable raster files, source labels, rights, captions, and contact details only when verified. No font binaries. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-01 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/press/cover.png` — 1600×900; generated_art_export.
- `brand/assets/press/index.html` — responsive asset catalog; functional_document.

Raw generation outputs: `brand/sources/generated/MKT-09/`. Final Astra prompt records: `brand/prompts/astra/MKT-09/`. Generation provenance: `brand/provenance/generations/MKT-09/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## DOC-01 — Runtime architecture diagram

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DOCS, GITHUB.

### Brief supplied to Astra

Source-verified harness→MCP→gateway→mod→host boundary diagram. Show protocol/core beside runtime flow, not a fictitious network hop. New publisher stays read-only. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/docs/runtime-architecture-master.png` — 1600×900; generated_art_export.
- `brand/assets/docs/runtime-architecture.png` — 2400×1350; generated_art_export.

Raw generation outputs: `brand/sources/generated/DOC-01/`. Final Astra prompt records: `brand/prompts/astra/DOC-01/`. Generation provenance: `brand/provenance/generations/DOC-01/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_architecture_owner_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## DOC-02 — Publication and evidence-flow diagram

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DOCS, RUN.

### Brief supplied to Astra

Approved sanitized source→validation→review→public run/card/media. Depict private and public boundaries distinctly; labels factual to implemented system. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/docs/publication-flow.png` — 1600×1000; generated_art_export.

Raw generation outputs: `brand/sources/generated/DOC-02/`. Final Astra prompt records: `brand/prompts/astra/DOC-02/`. Generation provenance: `brand/provenance/generations/DOC-02/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_publisher_owner_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## DOC-03 — Repository and rename map

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DOCS, GITHUB.

### Brief supplied to Astra

Generate from actual repository-plan state, marking planned versus completed renames and preserving active map-job constraints. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/docs/repository-map.png` — 1600×1200; generated_art_export.

Raw generation outputs: `brand/sources/generated/DOC-03/`. Final Astra prompt records: `brand/prompts/astra/DOC-03/`. Generation provenance: `brand/provenance/generations/DOC-03/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_migration_state_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Describe the actual approved state/result; include source link and equivalent accessible text. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## DOC-04 — Three-level orchestration diagram

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DOCS.

### Brief supplied to Astra

Show root, leads, coordinators, and leaf implementer/reviewers with max 250 open descendants. Label architecture diagram as planned unless native IDs prove an executed trace. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/docs/agent-tree.png` — 1800×1400; generated_art_export.

Raw generation outputs: `brand/sources/generated/DOC-04/`. Final Astra prompt records: `brand/prompts/astra/DOC-04/`. Generation provenance: `brand/provenance/generations/DOC-04/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## DOC-05 — Maintainer and contributor onboarding graphic

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** DOCS, GITHUB.

### Brief supplied to Astra

Working paths from tested recipe to focused issue, branch, tests, review, and contribution; do not invent missing installation tooling. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets None; inspect current rights-reviewed context. only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/docs/contributor-path.png` — 1200×900; generated_art_export.

Raw generation outputs: `brand/sources/generated/DOC-05/`. Final Astra prompt records: `brand/prompts/astra/DOC-05/`. Generation provenance: `brand/provenance/generations/DOC-05/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-01-B — Ascension repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'Ascension', role 'Agent-run coordination, structured records, and replay; starting with Slay the Spire 2.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: ascension. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/ascension-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-01-B/`. Final Astra prompt records: `brand/prompts/astra/GH-01-B/`. Generation provenance: `brand/provenance/generations/GH-01-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-01-S — Ascension social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for ascension. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-01-S/`. Final Astra prompt records: `brand/prompts/astra/GH-01-S/`. Generation provenance: `brand/provenance/generations/GH-01-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-02-B — Slay the Spire 2 MCP repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'Slay the Spire 2 MCP', role 'MCP tools for the controlled STS2 gateway interface.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: sts2-mcp. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-mcp-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/sts2-mcp-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-02-B/`. Final Astra prompt records: `brand/prompts/astra/GH-02-B/`. Generation provenance: `brand/provenance/generations/GH-02-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-02-S — Slay the Spire 2 MCP social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for sts2-mcp. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-mcp-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-02-S/`. Final Astra prompt records: `brand/prompts/astra/GH-02-S/`. Generation provenance: `brand/provenance/generations/GH-02-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-03-B — Slay the Spire 2 Mod repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'Slay the Spire 2 Mod', role 'Native game integration for controlled observation and actions.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: sts2-mod. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-mod-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/sts2-mod-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-03-B/`. Final Astra prompt records: `brand/prompts/astra/GH-03-B/`. Generation provenance: `brand/provenance/generations/GH-03-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-03-S — Slay the Spire 2 Mod social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for sts2-mod. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-mod-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-03-S/`. Final Astra prompt records: `brand/prompts/astra/GH-03-S/`. Generation provenance: `brand/provenance/generations/GH-03-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-04-B — STS2 Domain Core repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'STS2 Domain Core', role 'Typed STS2 state and validation primitives.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: sts2-core. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-core-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/sts2-core-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-04-B/`. Final Astra prompt records: `brand/prompts/astra/GH-04-B/`. Generation provenance: `brand/provenance/generations/GH-04-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-04-S — STS2 Domain Core social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for sts2-core. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-core-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-04-S/`. Final Astra prompt records: `brand/prompts/astra/GH-04-S/`. Generation provenance: `brand/provenance/generations/GH-04-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-05-B — STS2 Gateway repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'STS2 Gateway', role 'Game-instance routing, lifecycle boundaries, and request authority.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: sts2-gateway. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-gateway-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/sts2-gateway-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-05-B/`. Final Astra prompt records: `brand/prompts/astra/GH-05-B/`. Generation provenance: `brand/provenance/generations/GH-05-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-05-S — STS2 Gateway social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for sts2-gateway. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-gateway-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-05-S/`. Final Astra prompt records: `brand/prompts/astra/GH-05-S/`. Generation provenance: `brand/provenance/generations/GH-05-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-06-B — STS2 Protocol repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'STS2 Protocol', role 'Shared metadata contracts and conformance artifacts.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: sts2-protocol. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-protocol-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/sts2-protocol-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-06-B/`. Final Astra prompt records: `brand/prompts/astra/GH-06-B/`. Generation provenance: `brand/provenance/generations/GH-06-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-06-S — STS2 Protocol social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for sts2-protocol. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-protocol-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-06-S/`. Final Astra prompt records: `brand/prompts/astra/GH-06-S/`. Generation provenance: `brand/provenance/generations/GH-06-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-07-B — STS2 Map Viewer repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'STS2 Map Viewer', role 'Player-visible map topology and route inspection.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: sts2-map. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-map-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/sts2-map-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-07-B/`. Final Astra prompt records: `brand/prompts/astra/GH-07-B/`. Generation provenance: `brand/provenance/generations/GH-07-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-07-S — STS2 Map Viewer social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for sts2-map. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/sts2-map-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-07-S/`. Final Astra prompt records: `brand/prompts/astra/GH-07-S/`. Generation provenance: `brand/provenance/generations/GH-07-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-08-B — Ascension Watchdog repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'Ascension Watchdog', role 'Source-validated deployment supervision and recovery for Ascension.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: ascension-watchdog. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-watchdog-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/ascension-watchdog-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-08-B/`. Final Astra prompt records: `brand/prompts/astra/GH-08-B/`. Generation provenance: `brand/provenance/generations/GH-08-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-08-S — Ascension Watchdog social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for ascension-watchdog. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-watchdog-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-08-S/`. Final Astra prompt records: `brand/prompts/astra/GH-08-S/`. Generation provenance: `brand/provenance/generations/GH-08-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-09-B — Ascension Observability repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'Ascension Observability', role 'Self-hosted observability integrations for AI Ascension experiments.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: ascension-observability. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-observability-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/ascension-observability-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-09-B/`. Final Astra prompt records: `brand/prompts/astra/GH-09-B/`. Generation provenance: `brand/provenance/generations/GH-09-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-09-S — Ascension Observability social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for ascension-observability. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-observability-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-09-S/`. Final Astra prompt records: `brand/prompts/astra/GH-09-S/`. Generation provenance: `brand/provenance/generations/GH-09-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-10-B — AI Ascension Website repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'AI Ascension Website', role 'The canonical Watch and Build experience for AI Ascension.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: website. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/website-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/website-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-10-B/`. Final Astra prompt records: `brand/prompts/astra/GH-10-B/`. Generation provenance: `brand/provenance/generations/GH-10-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-10-S — AI Ascension Website social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for website. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/website-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-10-S/`. Final Astra prompt records: `brand/prompts/astra/GH-10-S/`. Generation provenance: `brand/provenance/generations/GH-10-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-11-B — AI Ascension Historical Evidence repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'AI Ascension Historical Evidence', role 'Historical evidence, stable proof URLs, and the legacy entry point.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: AI-Ascension.github.io. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ai-ascensiondot-githubdot-io-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/ai-ascensiondot-githubdot-io-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-11-B/`. Final Astra prompt records: `brand/prompts/astra/GH-11-B/`. Generation provenance: `brand/provenance/generations/GH-11-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-11-S — AI Ascension Historical Evidence social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for AI-Ascension.github.io. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ai-ascensiondot-githubdot-io-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-11-S/`. Final Astra prompt records: `brand/prompts/astra/GH-11-S/`. Generation provenance: `brand/provenance/generations/GH-11-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-12-B — AI Ascension Community repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'AI Ascension Community', role 'Organization profile, contributor guidance, governance, and shared presentation.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: .github. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/dot-github-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/dot-github-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-12-B/`. Final Astra prompt records: `brand/prompts/astra/GH-12-B/`. Generation provenance: `brand/provenance/generations/GH-12-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-12-S — AI Ascension Community social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for .github. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/dot-github-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-12-S/`. Final Astra prompt records: `brand/prompts/astra/GH-12-S/`. Generation provenance: `brand/provenance/generations/GH-12-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-13-B — AI Ascension Brand System repository banner

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB.

### Brief supplied to Astra

One family template, exact display title 'AI Ascension Brand System', role 'Canonical brand sources, publication tooling, campaign kit, and rollout evidence.', and a link in README text to the flagship. Do not bake stale capability results into decorative banner art. Target repository: ascension-brand-overhaul. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-brand-overhaul-banner-light.png` — 1600×400; generated_art_export.
- `brand/assets/github/ascension-brand-overhaul-banner-dark.png` — 1600×400; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-13-B/`. Final Astra prompt records: `brand/prompts/astra/GH-13-B/`. Generation provenance: `brand/provenance/generations/GH-13-B/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


## GH-13-S — AI Ascension Brand System social preview

**Author:** `W02-C1-BUILD`, actual `gpt-6-astra` / `max` agent. **Image model:** `gpt-image-2`. **Surfaces:** GITHUB, SOCIAL_PREVIEW.

### Brief supplied to Astra

Repository social preview for ascension-brand-overhaul. Astra-specified exact title and concise role, with the approved gpt-image-2 mark as reference. Treat this size as the project target and test actual GitHub cropping before apply. Astra must independently author the final generation prompt and every creative revision; only gpt-image-2 may create or edit the artwork. The brief is not a pre-attested final prompt.

### Authoring and inputs

Use parent assets ID-02, BG-04 only when their approved generated masters/reference permissions are available. Independently write the actual composition, literal text, reference handling, color/style constraints, exclusions, and revision invariants. Verify source facts before including labels. Never use a generated image as empirical evidence. This is a task brief, not permission to copy or expose private inputs.

### Required exports

- `brand/assets/github/ascension-brand-overhaul-social.png` — 1280×640; generated_art_export.

Raw generation outputs: `brand/sources/generated/GH-13-S/`. Final Astra prompt records: `brand/prompts/astra/GH-13-S/`. Generation provenance: `brand/provenance/generations/GH-13-S/`. All creative variants and edit jobs receive distinct immutable records; unchanged resizes may share the same approved master.

### Review gate

astra_authorship_gpt_image_2_provenance_and_design_review. Inspect actual output at intended use size; literal labels and data against source; logo consistency; crop-safe layout; alpha if required; readable small-scale use; rights and model/authorship evidence. Decorative when adjacent text duplicates its meaning; otherwise a concise functional description. Independent review is separate from Astra's own inspection. Correct creative defects only with another Astra-authored gpt-image-2 generation/edit. Mark missing access or inputs by exact job/asset ID.


# Separately classified source-media and reference briefs

These are not artwork-generation assignments and do not loosen the all-art requirement. Any new graphic displayed alongside them must come from the pipeline above.

## RUN-06 — Real case-study screenshot set

**Classification:** evidence_media. **Method:** authentic_capture.

Capture or select three real approved frames from the chosen case: start/context, meaningful decision, actual outcome. Preserve original dimensions in source; scale only for exports with disclosure. This is evidence media, not generated artwork. Any surrounding promotional graphics must come from the Astra → gpt-image-2 art pipeline and remain separately identified.

Source: Resolve the actual approved source; never fabricate availability.

Gate: exact_capture_rights_and_publication_approval.

## RUN-07 — Real first-episode video

**Classification:** evidence_media. **Method:** authentic_capture.

Edit only actual approved footage/analysis; keep interventions and outcome truthful. Record cut list and source timestamps. Missing footage blocks this asset, not the report-only alternative. This is evidence media, not generated artwork. Any surrounding promotional graphics must come from the Astra → gpt-image-2 art pipeline and remain separately identified.

Source: Resolve the actual approved source; never fabricate availability.

Gate: media_rights_fact_check_and_publish_approval.

## RUN-08 — First-episode short clips

**Classification:** evidence_media. **Method:** authentic_capture.

Two truthful actual-footage clips with readable game context and a source run link. Generate separate exact per-clip caption outputs during implementation; no false causal editing. This is evidence media, not generated artwork. Any surrounding promotional graphics must come from the Astra → gpt-image-2 art pipeline and remain separately identified.

Source: Resolve the actual approved source; never fabricate availability.

Gate: media_rights_fact_check_and_publish_approval.

## OLD-01 — Historical gateway proof card

**Classification:** reference. **Method:** preserve_reference.

Preserve the historical proof card and the exact claim-to-fixture context. Do not relabel it as native gameplay. Do not ship this as new-brand artwork. Keep the historical archive unchanged for evidence and rollback.

Source: https://github.com/AI-Ascension/AI-Ascension.github.io/blob/main/assets/identity/proof-card.png

Gate: source_and_rights_verification.

## OLD-02 — Existing ledger-spire illustration

**Classification:** reference. **Method:** preserve_reference.

Inspect existing original/generated art provenance. Retain as historical/archive source; use only as a rights-reviewed reference for a new Astra-authored gpt-image-2 generation. Do not call it a screenshot. Do not ship this as new-brand artwork. Keep the historical archive unchanged for evidence and rollback.

Source: https://github.com/AI-Ascension/AI-Ascension.github.io/blob/main/assets/identity/art/ledger-spire-source.png

Gate: source_and_rights_verification.

## OLD-03 — Existing wordmark and glyph sources

**Classification:** reference. **Method:** preserve_reference.

Preserve reference to the current identity for rollback/comparison. The final new mark remains original and explicitly reviewed. Do not ship this as new-brand artwork. Keep the historical archive unchanged for evidence and rollback.

Source: https://github.com/AI-Ascension/AI-Ascension.github.io/blob/main/assets/identity/wordmark.svg

Gate: source_and_rights_verification.

## OLD-04 — Existing identity tokens

**Classification:** reference. **Method:** preserve_reference.

Reconcile and preserve the existing token source while building the new canonical version. Do not silently delete dark mode or reduced-motion behavior. Do not ship this as new-brand artwork. Keep the historical archive unchanged for evidence and rollback.

Source: https://github.com/AI-Ascension/AI-Ascension.github.io/blob/main/assets/identity/tokens.css

Gate: source_and_rights_verification.

## OLD-05 — Existing font notices (reference only)

**Classification:** reference. **Method:** preserve_reference.

Reference licenses and existing self-hosted deployment assets. Bundle no font binaries in the handoff ZIP or press kit. Do not ship this as new-brand artwork. Keep the historical archive unchanged for evidence and rollback.

Source: https://github.com/AI-Ascension/AI-Ascension.github.io/blob/main/assets/fonts/NOTICES.md

Gate: source_and_rights_verification.
