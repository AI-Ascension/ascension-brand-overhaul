# GitHub presentation handoff

The public identity is **AI Ascension**. The flagship developer product is
**Ascension**, currently served by
[AI-Ascension/sts2-harness](https://github.com/AI-Ascension/sts2-harness). The
recurring run and research series is **The Climb — by AI Ascension**. The
GitHub handle remains `AI-Ascension`, and the canonical public site remains
<https://aiascension.tech/>.

## Prepared source changes

W04 prepared isolated branches named `work/w04-presentation` from the exact
heads recorded in `github/presentation-manifest.json`:

- `.github/profile/README.md` and `.github/README.md` introduce the hierarchy
  and preserve policies, issue forms, governance, evidence, and disclaimer
  links.
- `sts2-harness/README.md` presents **Ascension** while retaining the current
  `sts2-harness` URL and runtime identifiers.
- `sts2-game-core`, `sts2-game-mod`, `sts2-gateway`, `sts2-mcp-server`, and
  `sts2-protocol` identify their bounded responsibility as Ascension
  components. `ascension-watchdog` and `ai-agent-observability` receive the
  same bounded presentation treatment.
- `AI-Ascension.github.io/README.md` and the entry hero copy identify the
  historical Pages site. Its proof, evidence, recipe, architecture,
  repository, contribution, anchor, and asset paths remain in place; the
  static site was not rebuilt or deployed.
- No source/runtime package, binary, environment, schema, protocol, or
  evidence identity was renamed. Current repository URLs remain the links until
  an approved GitHub rename.

The actual changed files and base revisions are recorded in the manifest. Each
companion remains a separate branch and repository; this brand repository does
not copy runtime source.

## Settings and publication boundary

Repository descriptions, topics, pins, visibility, default branches, Pages
source, and hosted Action settings are GitHub settings. The presentation
manifest records proposed values and a manual checklist only. No settings
mutation, branch push, merge, rename, deployment, owner notification, or
campaign send occurred during this pass.

The migration plan uses stable IDs and current heads rather than names alone.
It blocks an active assignment, stale source state, protected-config drift,
name collision, hosted Action caller, unavailable inventory, and missing
authority. The map visualizer remains explicitly deferred while its active
assignment pins `ascension-map-visualizer`.

## Art and evidence boundary

This pass introduces no artwork. Existing Pages assets, historical proof
routes, and evidence links remain untouched. Identity artwork remains blocked
until a genuine depth-three Astra author and verified `gpt-image-2` generation
record are available. Existing visual assets are never relabeled as new
generated art or gameplay proof.
