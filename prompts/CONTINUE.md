# Resume the same implementation

Read MASTER_PROMPT.md, AGENTS.md, the actual root state/task/spawn/approval ledgers, current Git status and PRs, source HEADs, and the existing asset manifest. Reconcile native open threads and write leases before spawning. Retain requested gpt-5.6-luna/max for descendants and actual depth ≤3.

Continue the smallest incomplete dependency in the existing task graph. Do not repeat completed renames, create duplicate repos/PRs, overwrite approved sources, regenerate approved art, or rerun publication as a side effect of local validation. Revalidate cached inputs when source revisions changed. Preserve old evidence and approval history; changed content requires a new digest-bound review.

Complete every unblocked implementation, repair, and independent test. A missing external permission blocks only that action. Report the exact blocker and delivered work without pretending the entire assignment is complete. Use the final report template only when reconciled with actual ledgers.

## Mandatory artwork source

All new artwork, including promotional images, exported diagrams, card artwork, icons, and creative revisions, follows `art/ASTRA_GPT_IMAGE_2_POLICY.md`: an actual `gpt-6-astra` author writes each final prompt and `gpt-image-2` generates the art. Functional HTML/CSS/data and approved real evidence are separate. Runtime serving uses already approved images with no implicit model calls. Missing art-model access blocks that asset, never authorizes code-drawn or reused-art substitutions.
