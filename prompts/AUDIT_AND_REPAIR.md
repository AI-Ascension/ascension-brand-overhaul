# Adversarial completion audit and repair

Act as the root review/integration authority for the completed implementation, not a strategy consultant. Read MASTER_PROMPT.md, the complete requirement matrix, art registry, source pins, target policy files, PR diffs, and actual test evidence. Spawn independent Luna/max reviewers through the required depth structure within the global budget; do not accept builder self-review.

Attempt to falsify each claim of completion. Inspect the actual site at mobile/desktop/light/dark, every primary CTA and form state, source-qualified run evidence, actual asset exports and small-size crops, publication negative cases, migration idempotency/rollback, current repository IDs, and runtime parent/model records. Look for dead buttons, synthetic production content, invented statistics, missing art, stale provider rosters, unsupported compatibility, privacy leaks, and tests only planned rather than run.

Route defects to separate bounded fixers. Re-run impacted checks and integration checks after repair. Never approve your own new patch without a different reviewer. Reconcile all REQ and asset IDs, and produce an exact implemented/verified/blocked matrix. Do not mark missing native model metadata, host access, footage, or live authorization as passed. Finish all independent work and assemble the actual release handoff.

## Mandatory artwork source

All new artwork, including promotional images, exported diagrams, card artwork, icons, and creative revisions, follows `art/ASTRA_GPT_IMAGE_2_POLICY.md`: an actual `gpt-6-astra` author writes each final prompt and `gpt-image-2` generates the art. Functional HTML/CSS/data and approved real evidence are separate. Runtime serving uses already approved images with no implicit model calls. Missing art-model access blocks that asset, never authorizes code-drawn or reused-art substitutions.
