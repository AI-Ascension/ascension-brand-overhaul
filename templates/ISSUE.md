# [Bounded implementation task]

Workstream / coordinator / requirement IDs:
Authenticated implementing account (assign when permitted):
Current source ID and commit:
Objective:
Allowed write scope:
Dependencies:
Required actual outputs:
Acceptance criteria and negative cases:
Independent review owner:
Evidence path:
Permissions required for external operations:

Do not create a duplicate issue if the same bounded task already exists. Preserve links across repository renames by stable repository/issue identity.
