# Local publication destination

The offline publisher writes approved local exports below this directory at
`<public_run_id>/v<publication_version>/`. No public run has been exported by
this worktree. Synthetic fixtures live under `tests/fixtures/publication/` and
must fail the production gate.
